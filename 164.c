#include<stdio.h>
#include<conio.h>
const int tamanho = 5;
void main(){
	int k, vet1[tamanho], vet2[tamanho], vet3[tamanho*2];
	printf("Informe os %d valores do primeiro vetor: ", tamanho);
	for(k = 0; k < tamanho; k++){
		scanf("%d", &vet1[k]);
	}
	printf("Informe os %d valores do segundo vetor: ");
	for(k = 0; k < tamanho; k++){
		scanf("%d", &vet2[k]);
	}
	for(k = 0; k < tamanho; k++){
		
	}
}
